# GOTCHAS · 踩坑记录

> 本文件记录在本项目协作中实际发生过的错误与教训。
> 灵感来自资深开发者的共识做法：**每当 AI 犯一个错，就在这里记一条**。
> 时间久了，这份清单会成为提升协作质量、信噪比最高的内容。
>
> 记录格式固定为三段：**现象（发生了什么）→ 根因（为什么会发生）→ 防护（以后怎么避免）**。

---

## 元规则：为什么需要这个文件

资深开发者使用 AI 编程助手的两条核心共识：

1. **"你的领域专业知识才是瓶颈，不是 AI 工具。"**
   —— AI 能干活，但判断对错、把关方向的人是你。

2. **"CLAUDE.md 能引导行为，但不能强制执行 —— AI 仍可能忽略它。"**
   —— 所以规则要写下来（降低犯错率），关键的还要靠 hooks 物理强制（杜绝犯错）。
   —— 这也是本文件存在的意义：把"踩过的坑"显式记录，比指望 AI"记住"更可靠。

---

## 坑 #1 · 污染了 conda base 环境（环境隔离）

- **现象**：项目初期，AI 直接用用户的 miniconda **base 环境**运行服务并 `pip install`
  （装入了 fastapi、uvicorn、jieba、pymupdf、pytest 等一批包），未创建独立虚拟环境。
  其中 `pydantic` 的版本还可能因此被顶替。

- **根因**：AI 图省事，跳过了"为项目建独立环境"这一基本步骤。
  base 环境是全局共享的，往里装包会影响用户所有其他项目，违反**环境隔离原则**。

- **防护**：
  1. 任何 Python 项目**第一步**先建独立环境：`python -m venv .venv`
  2. 所有 python/pip 命令走该环境解释器：`.\.venv\Scripts\python.exe ...`（Windows）
  3. 运行前用 `python -c "import sys; print(sys.executable)"` 确认在 `.venv` 内
  4. `.venv/` 必须写进 `.gitignore`
  5. 已写入全局规则 `~/.claude/CLAUDE.md` 与跨项目记忆，确保以后所有项目生效

---

## 坑 #2 · 服务器只监听 127.0.0.1，局域网/外部访问不了

- **现象**：用默认参数启动 uvicorn 后，`localhost:8000` 能开，但用局域网 IP
  （如 `192.168.x.x:8000`）打不开。

- **根因**：uvicorn 默认只绑定回环地址 `127.0.0.1`，不监听其他网卡，
  所以本机以外（局域网其他设备、外部访问）都连不上。

- **防护**：需要对外访问时，启动必须加 `--host 0.0.0.0` 绑定所有网卡：
  `uvicorn app.main:app --host 0.0.0.0 --port 8000`
  （另外注意 Windows 防火墙可能拦截入站端口，必要时放行）

---

## 坑 #3 · 后台进程被挂起 / 僵尸进程占用端口

- **现象**：Locust 压测进程用后台方式启动后变成 `suspended (tty output)` 假死状态；
  之后重启一直报 `Address already in use: 8089`，普通 `kill` 杀不掉。

- **根因**：
  1. 后台进程尝试往终端输出被系统暂停（suspended）；
  2. 被暂停的进程收到普通 `kill` 信号但不处理，端口一直被占着。

- **防护**：
  1. 后台跑服务用 `nohup ... > /tmp/xxx.log 2>&1 &`，把输出重定向到文件，避免 tty 挂起
  2. 顽固进程用 `kill -9 <pid>` 强制结束
  3. 启动前先 `ss -tlnp | grep <端口>` 确认端口已释放

---

## 坑 #4 · 切换了 Python 版本导致命令找不到包

- **现象**：`pyenv shell 3.11.1` 切换版本后，`locust`、`claude` 等命令报
  `command not found` 或 `No module named`。

- **根因**：包是装在切换前的那个 Python 版本里的，切到新版本后新环境没有这些包。

- **防护**：固定项目使用一个 Python 环境（最好就是 `.venv`），不随意切换；
  切换后若缺包，要么装回，要么切回原版本。

---

## 长期实践提醒（来自资深开发者共识，非本项目已发生的坑）

这些不是已经踩的坑，但记在这里防患未然：

- **上下文别超 60%**：会话过长时 AI 性能断崖下跌、忽略指令。
  做正经开发前，建议**开新会话**，让 AI 从干净上下文 + CLAUDE.md + 本文件开始。
- **CLAUDE.md 要短（<200 行）**：塞太多 AI 会选择性忽略，尤其文件末尾的内容。
- **复杂任务先规划**：让 AI 先把方案讲清楚、反问边界情况，确认后再写代码，避免方向错了全部返工。
- **偏离方向就回滚重来**，别在原地纠正——错误推理留在上下文里会继续误导 AI。

---

*维护方式：以后每发生一次值得记的错误，按"现象→根因→防护"三段式在上方追加一条。*
